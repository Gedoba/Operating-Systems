class BrowsableView {
    Iterator i = view.getIterator(x, y);

    foreach(var element in Views) {
        element = i.next();
        element.method(); // .click();

    }


    next(){
        foreach c in children{
            c ? contains point

        }
    }
}


//observer